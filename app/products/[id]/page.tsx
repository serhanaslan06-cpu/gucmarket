import {products,upsCriteria} from '@/lib/data'; import Link from 'next/link';
export default async function Product({params}:{params:Promise<{id:string}>}){const {id}=await params; const p=products.find(x=>x.id===id)||products[0]; const t=p.technical; return <main className="container" style={{paddingTop:35}}><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:30}}><div className="card" style={{minHeight:450,display:'grid',placeItems:'center',fontSize:140}}>▣</div><div><div style={{color:'#17856f',fontWeight:800}}>🟢 DOĞRULANMIŞ TEDARİKÇİ</div><h1 style={{fontSize:40}}>{p.name}</h1><p style={{fontSize:17,color:'var(--muted)'}}>{p.spec}</p><div style={{fontSize:30,fontWeight:800,margin:'25px 0'}}>{p.price}</div><p style={{color:'var(--muted)'}}>+ KDV'den başlayan fiyatlarla</p><div className="card" style={{padding:20,marginTop:20}}><b>{p.seller}</b><p>📍 {p.city}</p><p>Yetkili tedarikçi • Ürün ve teknik destek</p><div style={{display:'flex',gap:10}}><Link href="/services/new" style={{padding:13,background:'var(--blue)',color:'#fff',borderRadius:9,textDecoration:'none',fontWeight:800}}>📩 Teklif Al</Link><button style={{padding:13}}>📞 İletişim Bilgilerini Gör</button></div></div></div></div><section style={{marginTop:40}}><h2>Teknik Özellikler</h2><div className="card" style={{padding:20}}>{t? <div style={{display:'grid',gridTemplateColumns:'repeat(2,1fr)',gap:12}}>{upsCriteria.map(c=><div key={c.key} style={{padding:'10px 0',borderBottom:'1px solid #edf0f5'}}><b>{c.label}:</b> {Array.isArray(t[c.key as keyof typeof t])? (t[c.key as keyof typeof t] as string[]).join(', ') : String(t[c.key as keyof typeof t] ?? '-')} {c.unit||''}</div>)}</div>:<><p><b>Marka:</b> {p.brand}</p><p><b>Kategori:</b> {p.cat}</p><p><b>Model / Ürün:</b> {p.name}</p><p><b>Satıcı:</b> {p.seller}</p></>}</div></section></main>}
